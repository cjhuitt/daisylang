foo = [1, 1, 2, 3]
bar = [2 + 3, 3 + 5, 5 + 8]
foobar = foo + bar
if foobar?
    if foobar.empty?()
        print( "Nothing got added?!" )
    print( foobar.count().toString() + " items: " + foobar.toString() )

tokens = []
tokens.append!( Class, Contract, Method, Array )
for entry in tokens
    print( entry )

