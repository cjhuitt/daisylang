foo = [1, 1, 2, 3]
bar = [2 + 3, 3 + 5, 5 + 8]
foobar = foo + bar
print( foobar.empty? )
print( foobar.size )
print( foobar )
for entry in foobar
    print( entry )

names = String[]
names.append!( "Daisy" )

tokens = Stringifiable[]
tokens.append!( [Class, Contract, Function, Array] )
print( tokens )
